// Only one unit for now; kept as a list (with the same divisor shape as pressureUnits.js)
// so the dropdown format matches the other fields and more units can be added later.
export const DENSITY_UNITS = [{ id: 'kg_m3', label: 'kg/m³', divisor: 1 }]
